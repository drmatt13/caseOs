{
  "name": "oauth-callback",
  "version": "1.0.0",
  "description": "A Lambda function to exchange Cognito OAuth codes and establish app sessions.",
  "main": "index.ts",
  "repository": "",
  "author": "Matthew Sweeney",
  "license": "UNLICENSED",
  "scripts": {
    "unit": "jest",
    "lint": "eslint '*.ts' --quiet --fix",
    "compile": "tsc",
    "test": "npm run compile && npm run unit"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.930.0",
    "@repo/database": "*",
    "@repo/shared-lambda-utils": "*",
    "esbuild": "^0.27.4",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.161",
    "@types/node": "^25.6.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
