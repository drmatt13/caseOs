{
  "name": "get-user",
  "version": "1.0.0",
  "description": "A Lambda function to return the current authenticated user from the database.",
  "main": "index.ts",
  "repository": "",
  "author": "Matthew Sweeney",
  "license": "UNLICENSED",
  "scripts": {
    "unit": "jest",
    "lint": "eslint '*.ts' --quiet --fix",
    "compile": "tsc",
    "test": "npm run compile && npm run unit"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.0.0",
    "@aws-sdk/client-secrets-manager": "^3.930.0",
    "@repo/database": "*",
    "cookie": "^0.7.2",
    "esbuild": "^0.27.4",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.161",
    "@types/cookie": "^0.6.0",
    "@types/node": "^25.6.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
