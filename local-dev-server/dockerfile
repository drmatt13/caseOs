FROM node:alpine

WORKDIR /workspace

EXPOSE 8080

# Install Linux-native deps at workspace root (npm workspaces), then start dev server
CMD ["sh", "-c", "npm install --prefer-offline && cd local-dev-server && npm run dev"]